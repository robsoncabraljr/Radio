<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rpfm.projetoandroid.com.radiopopularlivre_rpfm">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.TelaSplashActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/titulo_activity_login"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/titulo_activity_login"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.CadastroOuvintesActivity"
            android:label="@string/titulo_activity_cadastro"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.ContatosActivity"
            android:label="@string/app_name_contato"
            android:screenOrientation="portrait" />
        <activity android:name=".activity.SobreActivity" />

        <service android:name=".service.MediaPlayerService" />

    </application>

</manifest>